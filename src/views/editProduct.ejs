<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chỉnh Sửa Thông Tin Người Dùng</title>
    <!-- Thêm các thư viện CSS và JavaScript nếu cần -->
</head>
<body>
    <h2>Chỉnh Sửa Thông Tin Hàng </h2>
    <form action="/api/edit/<%= product.id %>" method="POST">

        <label for="title">Ten Hang Hoa: </label>
        <input type="text" id="title" name="title" value="<%= product.title %>" required><br>

        <label for="price">Gia: </label>
        <input type="text" id="price" name="price" value="<%= product.price %>"  required><br>

        <label for="discount">Giam Gia:</label>
        <input type="text" id="discount" name="discount" value="<%= product.discount %>"  required><br>

        <label for="description">Mo Ta:</label>
        <input type="text" id="description" name="description" value="<%= product.description %>"  required><br>

        <label for="category_id">Loai San Pham:</label>
        <select id="category_id" name="category_id" required>
            <% categoryName.forEach(category => { %>
                <option value="<%= category.id %>"><%= category.name %></option>
            <% }); %>
        </select><br>

        <button type="submit">Thêm Sản Phẩm: </button>
    </form>
</body>
</html>
